if false
  greeting = “hello world”
end

greeting

# this will return a nil because the if statement wont assign anything because its false and greeting has no assigned value, but since it was initialized in a local variable, it wont throw an exception